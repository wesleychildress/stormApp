import { EsriLoaderService } from 'angular-esri-loader';
export declare class EsriModuleProvider {
    private esriLoader;
    constructor(esriLoader: EsriLoaderService);
    require(moduleNames: string[]): Promise<any[]>;
}
